
desc "List all TODOs"
task :todos do
  puts `grep -R "TODO" src/`
end

desc "List all HACKs"
task :hacks do
  puts `grep -R "HACK" src/`
end

desc "List all FIXMEs"
task :fixmes do
  puts `grep -R "FIXME" src/`
end

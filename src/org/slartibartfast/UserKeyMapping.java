package org.slartibartfast;

import java.util.HashMap;

/**
 * Map of events to keys, defined by the user.
 *
 * This is a wrapper class around a String,String hash map so we
 * don't have to constantly type out the types.
 */
public class UserKeyMapping extends HashMap<Events, String> {

}
